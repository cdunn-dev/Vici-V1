{
  "name": "running-analytics-server",
  "version": "1.0.0",
  "description": "Running analytics platform server",
  "main": "index.ts",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@types/jest": "^29.5.12",
    "vitest": "^1.3.1",
    "@types/supertest": "^6.0.2",
    "supertest": "^6.3.4"
  }
}